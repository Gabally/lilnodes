{
  "name": "code-runner-worker",
  "version": "0.0.1",
  "description": "program that executes untrusted code from inside a docker container, and passes a JSON object to the function from stdin and returns the result on stdout",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js"
  },
  "author": "Gabriele Morisano",
  "license": "MIT",
  "dependencies": {
    "vm2": "^3.9.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
