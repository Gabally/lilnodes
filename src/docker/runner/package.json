{
  "name": "code-runner-worker",
  "version": "0.0.1",
  "description": "program that executes untrusted code from inside a docker container, and passes a JSON object to the function from stdin and returns the result to stdout",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/index.ts",
    "build": "tsc",
    "start": "ts-node ./src/index.ts"
  },
  "author": "Gabriele Morisano",
  "license": "MIT",
  "dependencies": {
    "vm2": "^3.9.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
